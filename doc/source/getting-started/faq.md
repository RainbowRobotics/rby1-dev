# FAQ

Q. What is the minimal first‑run flow?  
A. Power on → hotspot + Web UI check → connect to external AP → install SDK → read state → learn E‑Stop behavior.

Q. Can I start over Wi‑Fi?  
A. Yes, but a wired link is recommended for the first connection.

Q. Which port should I use for the SDK?  
A. Use the default service port shipped with your build; confirm in your Web UI or release notes. TODO: fill the default here.

Q. Where is a complete state example?  
A. See `examples/python/03_robot_state.py` in the SDK repository.
